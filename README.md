
  # High-Fidelity Prototype for BeanExchange

  This is a code bundle for High-Fidelity Prototype for BeanExchange. The original project is available at https://www.figma.com/design/nXHQImZhQYsYuwjLJxTbrY/High-Fidelity-Prototype-for-BeanExchange.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  